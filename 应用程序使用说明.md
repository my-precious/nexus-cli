# Nexus CLI 自动重启应用程序使用说明

## 概述
这是一个 macOS 桌面应用程序，用于自动启动和管理 Nexus CLI 的批量处理任务。应用程序提供了用户友好的界面，支持自动重启、错误处理和系统检查功能。

## 安装步骤

### 1. 运行安装脚本
```bash
cd ~/nexus-cli
./install_nexus_app.sh
```

### 2. 手动安装（可选）
如果不想使用安装脚本，可以手动将应用程序复制到桌面：
```bash
cp -R "Nexus CLI Restart.app" ~/Desktop/
```

## 使用前准备

### 1. 编译 Nexus CLI
```bash
cd ~/nexus-cli/clients/cli
cargo build --release
```

### 2. 创建节点列表文件
```bash
mkdir -p ~/.nexus
echo "你的节点ID列表，每行一个" > ~/.nexus/nodes.txt
```

示例节点文件内容：
```
10001
10002
10003
10004
```

## 使用方法

### 启动应用程序
1. 双击桌面上的 "Nexus CLI Restart" 图标
2. 应用程序会自动检查系统要求
3. 如果检查通过，会自动启动 Nexus CLI

### 停止应用程序
- 在终端窗口中按 `Ctrl+C`
- 应用程序会优雅地关闭所有节点

## 功能特性

### ✅ 自动重启
- 每 40 分钟自动重启一次
- 重启前等待 20 秒优雅关闭时间
- 支持无限循环重启

### ✅ 系统检查
- 自动检查 CLI 可执行文件是否存在
- 自动检查节点列表文件是否存在
- 提供详细的错误信息和建议

### ✅ 用户友好界面
- 彩色输出，信息清晰
- 实时状态显示
- 错误处理和提示

### ✅ 持久化统计
- 证明数量自动保存到 `~/.nexus/proof_stats.json`
- 重启后自动恢复统计信息
- 支持每个节点的独立统计

## 配置说明

### 当前配置
- **重启间隔**: 40 分钟
- **优雅关闭时间**: 20 秒
- **最大并发节点数**: 80
- **节点文件位置**: `~/.nexus/nodes.txt`

### 修改配置
如需修改配置，请编辑应用程序包内的脚本文件：
```bash
# 右键点击应用程序 -> 显示包内容
# 编辑文件: Contents/MacOS/nexus_restart
```

## 故障排除

### 常见问题

#### 1. "找不到 Nexus CLI 可执行文件"
**解决方案**: 确保已编译 CLI
```bash
cd ~/nexus-cli/clients/cli
cargo build --release
```

#### 2. "找不到节点列表文件"
**解决方案**: 创建节点文件
```bash
mkdir -p ~/.nexus
echo "你的节点ID" > ~/.nexus/nodes.txt
```

#### 3. 应用程序无法启动
**解决方案**: 检查权限
```bash
chmod +x ~/Desktop/Nexus\ CLI\ Restart.app/Contents/MacOS/nexus_restart
```

### 日志文件
应用程序运行时的日志会显示在终端窗口中，包括：
- 系统检查结果
- CLI 启动状态
- 错误信息
- 统计信息

## 高级功能

### 自定义配置
可以通过修改脚本文件来自定义：
- 重启间隔时间
- 优雅关闭等待时间
- 最大并发节点数
- 节点文件路径

### 后台运行
如果需要后台运行，可以使用：
```bash
nohup ~/Desktop/Nexus\ CLI\ Restart.app/Contents/MacOS/nexus_restart > nexus.log 2>&1 &
```

## 技术支持

如有问题，请：
1. 查看终端输出的错误信息
2. 检查系统要求是否满足
3. 确认配置文件是否正确

## 版本信息
- **版本**: 1.0
- **作者**: Nexus CLI Team
- **支持系统**: macOS 10.15+ 